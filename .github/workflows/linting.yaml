name: Linting Charts

on:
  push

jobs:
  lint-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        with:
          fetch-depth: 0

      - name: Set up chart-testing
        uses: helm/chart-testing-action@v2.1.0

      - name: Run chart-testing (lint-changed)
        id: list-changed
        run: |
          changed=$(ct list-changed)
          if [[ -n "$changed" ]]; then
            echo "::set-output name=changed::true"
          fi

      - name: Run chart-testing (lint)
        run: ct lint

      - name: Artifect Hub Lint
        run: |
          curl -LO https://github.com/artifacthub/hub/releases/download/v1.1.1/ah_1.1.1_linux_amd64.tar.gz
          tar xvzf ah_1.1.1_linux_amd64.tar.gz
          ./ah lint

